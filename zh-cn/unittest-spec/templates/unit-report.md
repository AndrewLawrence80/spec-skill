# Unit-test report: `<feature-slug>`

> 运行日期：YYYY-MM-DD - 探测到框架：`<framework>`（`<command used>`）

## Unit test for `<unit-name>`

### Cases & Coverage

- **覆盖用例：** happy path、edge cases（边界/空值/最大尺寸）、错误路径、以及 `data-model.md` 不变量。
- **覆盖率：** [如果运行了工具则填 lines/branches，否则写 "n/a"。]

### Issues & Gaps

- [ ] `<gap description>`: [<brief description>].
    - `Category`: [例如 "edge case"、"error path"、"data model invariant"、"race condition"].
    - `Scenarios`: [例如 "when input is empty"、"when network is unavailable"、"when user has no permissions"].
    - `Impact`: [例如 "low" / "medium" / "high"，并简述原因].
    - `TODO(<unit>): [How it will be addressed].

- [ ] `<issue description>`: [<brief description>].
    - `Category`: [例如 "edge case"、"error path"、"data model invariant"、"race condition"].
    - `Scenarios`: [例如 "when input is empty"、"when network is unavailable"、"when user has no permissions"].
    - `Impact`: [例如 "low" / "medium" / "high"，并简述原因].
    - `TODO(<unit>): [How it will be addressed].

### Open Questions

- [ ] `<question description>`: [<brief description>].
    - `Category`: [例如 "edge case"、"error path"、"data model invariant"、"race condition"].
