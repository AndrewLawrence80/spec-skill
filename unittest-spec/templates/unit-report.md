# Unit-test report: `<feature-slug>`

> Run on: YYYY-MM-DD - Framework detected: `<framework>` (`<command used>`)

## Unit test for `<unit-name>`

### Cases & Coverage

- **Cases covered:** happy path, edge cases (boundaries, empty/null, max sizes), error paths, and any `data-model.md` invariants.
- **Coverage:** [lines/branches if a tool ran, otherwise "n/a"].

### Issues & Gaps

- [ ] `<gap description>`: [<brief description>].
    - `Category`: [e.g., "edge case", "error path", "data model invariant","race condition"].
    - `Scenarios`: [e.g., "when input is empty", "when network is unavailable", "when user has no permissions"].
    - `Impact`: [e.g., "low: only affects a minor edge case", "medium: could cause incorrect behavior in some cases", "high: could cause crashes or data loss"].
    - `TODO(<unit>): [How it will be addressed].

- [ ] `<issue description>`: [<brief description>].
    - `Category`: [e.g., "edge case", "error path", "data model invariant","race condition"].
    - `Scenarios`: [e.g., "when input is empty", "when network is unavailable", "when user has no permissions"].
    - `Impact`: [e.g., "low: only affects a minor edge case", "medium: could cause incorrect behavior in some cases", "high: could cause crashes or data loss"].
    - `TODO(<unit>): [How it will be addressed].

### Open Questions

- [ ] `<question description>`: [<brief description>].
    - `Category`: [e.g., "edge case", "error path", "data model invariant","race condition"].
